```js
var obj = { foo: 'bar' };
var num = 1;
var arr = [ 1, 2, 3];
var regex = /foo/;
var undef = undefined;
var nil = null;

obj; // => { foo: 'bar' }
num; // => 1
arr; // => [ 1, 2, 3 ];
regex; // => /foo/;
undef; // => undefined
nil; // => null

Math.min(1, 2) // => 1
```
